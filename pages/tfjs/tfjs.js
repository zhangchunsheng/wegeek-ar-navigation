const tf = require('../../utils/tf.js');

Page({
    data: {
        text: "can't run tensorflow",
    },
    onReady: function () {
        this.setData({
            text: "can't run tensorflow",
        });
    },
});